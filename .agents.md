# AGENTS

## 协同开发项目

本项目（director-workbench）与以下项目是**协同开发**关系：

- **ComfyUI-MiniMax-H3-Long-Video**：`/media/hr/Data/Codes/ComfyUI/custom_nodes/ComfyUI-MiniMax-H3-Long-Video`

该 ComfyUI 自定义节点项目负责 MiniMax H3 长视频的分段生成（关键帧 FL2VA 分段、分镜提示词适配等），本工作台（director-workbench）为其提供导演/工作台侧的工具与工作流（如 `workflows/keyframe-video.template.json`）。两边的改动可能相互依赖，进行设计、接口或工作流变更时需考虑另一侧的兼容性。

## 提示词协议（MMH3 Prompt YAML v1）

分段/分镜提示词用统一 YAML 协议连接 skill、本工作台与 ComfyUI 节点：

- 协议规范：ComfyUI 仓库 `docs/mmh3-prompt-yaml-protocol.md`。
- 工作台侧实现：`src/prompt/protocol.ts`（类型 + 校验 + YAML 序列化），测试 `src/prompt/protocol.test.ts`。
- 工作流模板：`workflows/prompt-yaml.template.json`（把 `${prompt_yaml}` 透传到节点 `MMH3PromptYamlTest` 的单输入框）。
- prompt 节点内容即为协议 YAML 文本；生成时可把其作为 `prompt_yaml` 变量提交给 ComfyUI 解析节点。
