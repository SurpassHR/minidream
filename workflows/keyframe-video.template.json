{
  "1": {
    "class_type": "MiniMaxH3KeyframeVideo",
    "inputs": {
      "keyframes": "${keyframes}",
      "width": "${width}",
      "height": "${height}",
      "steps": "${steps}",
      "ref_seconds": "${ref_seconds}",
      "seam": "${seam}",
      "seed": "${seed}",
      "run_id": "${run_id}",
      "chain_previous_last": "${chain_previous_last}"
    }
  }
}
